import React from 'react'
import loadingDod from '../../assets/loading-dog.gif'

const Loader = () => {
          return (
                    <img src={loadingDod} alt=""  className='loader'/>
          )
}

export default Loader
